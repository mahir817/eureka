# Application Name
spring.application.name=eureka

# Server Configuration - Changed to 8081 because XAMPP uses 8080
server.port=8081

# Database Configuration (Optional - for demo we can skip database)
# Uncomment when you're ready to use MySQL
# spring.datasource.url=jdbc:mysql://localhost:3306/eureka?useSSL=false&serverTimezone=UTC
# spring.datasource.username=root
# spring.datasource.password=
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# CORS Configuration (allow frontend on port 3000)
# This will be configured in Java config class
