# Application Name
spring.application.name=eureka

# Server Configuration - Changed to 8081 because XAMPP uses 8080
server.port=8081

# Database Configuration - DISABLED for demo (no database needed yet)
# Spring Boot DataSource auto-configuration is excluded in EurekaApplication.java
# When you're ready to use MySQL, remove the exclude from EurekaApplication and uncomment below:
#
# spring.datasource.url=jdbc:mysql://localhost:3306/eureka?useSSL=false&serverTimezone=UTC
# spring.datasource.username=root
# spring.datasource.password=yourpassword
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# CORS Configuration (allow frontend on port 3000)
# This will be configured in Java config class
